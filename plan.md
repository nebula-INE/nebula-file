# 🎙️ VO-SE Pro Engine 開発計画（2026-2027）
## 〜「ドラッグ＆ドロップ音源適応型AI歌声合成」への道〜

## 0. コアコンセプト（最重要）

**「ユーザーは音源フォルダをD&Dするだけ。あとはAIがその声を覚えて歌う。」**

- **従来（WORLD）**: oto.iniのパラメータ（先行発声・オーバーラップ）とWAVの物理波形をその場で切り貼り＆ピッチシフト。
- **次世代（BigVGAN + Adaptation）**: 音源フォルダを**その場で解析（数分）**し、BigVGANがその音源の声質（スペクトル特性・非周期性・子音のアタック感）を**動的に学習（適応）**。その後はAIが高品質に歌唱合成。

**この機能が実現すれば、UTAU音源が「AI音源」に自動アップグレードされる世界が到来する。**

---

## 1. 技術アーキテクチャ（3ステップ適応プロセス）

### ステップ1: 音源フォルダの「音響指紋（Acoustic Fingerprint）」抽出
ユーザーがフォルダをD&Dした瞬間、バックグラウンドで以下の処理を実行：

| 処理内容 | 使用技術 / モジュール | 出力データ |
|----------|----------------------|------------|
| oto.ini パース | `modules/data/oto_parser.py` | 音素（alias）とWAVの対応表、先行発声/オーバーラップ値 |
| 全WAVの音響特徴量抽出 | `modules/tools/batch_voice_optimizer.py`（改良版） | **スペクトル包絡（メル頻度）**, **F0分布（平均・標準偏差）**, **非周期性指標（AP）の統計量**, **子音部のアタック時間** |
| 話者埋め込み（Embedding）ベクトル生成 | 軽量Encoder（例：ECAPA-TDNN や 簡易CNN） | **固定長ベクトル（例：256次元）** … これが「その音源の声の特徴ID」になる |

### ステップ2: BigVGANへの「Condition（条件）」注入
従来のBigVGANは「メルスペクトログラム」だけを入力としていたが、ここに**ステップ1で抽出した特徴量を追加で結合（Concatenate / FiLM / AdaIN）**します。

**ポイント**:
- この「音響指紋ベクトル」は、**その音源を特徴づける「声のクセ（太さ・息感・エッジ）」**を表します。
- 適応（学習）は、このベクトルを正しく出力できるようにEncoderを調整することと同義です。

### ステップ3: 適応学習（Adaptation）の実行方法（2つのモード）

| モード | 処理時間 | 品質 | 実装難易度 | ユーザー体験 |
|--------|----------|------|------------|--------------|
| **モードA: Zero-shot（推論時適応）** | **数秒** | ★★★☆☆ | 高 | D&D後、すぐに歌わせられる（待ち時間ゼロ） |
| **モードB: Few-shot Fine-tuning（追加学習）** | **5〜15分** | ★★★★★ | 中 | 最初だけ少し待つが、その音源に最適化された超高品質が得られる |

**理想**: モードAをデフォルトにし、ユーザーが「高品質モード」を選んだ場合のみモードBを実行する**ハイブリッド戦略**。

---

## 2. 開発ロードマップ（フェーズ別）

### フェーズ A（2026年 Q3）: 音響指紋抽出パイプラインの確立

| タスク | 詳細 | ゴール |
|--------|------|--------|
| **A-1** | `batch_voice_optimizer.py` を拡張し、**話者識別用の統計量（F0分布/スペクトル重心/非周期性の分散）**を出力する機能を追加。 | 音源フォルダをスキャンして「特徴JSON」を出力できること。 |
| **A-2** | 軽量Encoder（例：ECAPA-TDNNの小規模版）をPyTorch/ONNXで実装し、**A-1の統計量を入力 → 256次元ベクトルを出力**するモデルを構築。 | 異なるUTAU音源を入力すると、異なるベクトルが出力されることを確認。 |
| **A-3** | BigVGANのCondition入力層を改造し、**メルスペクトル + 256次元ベクトル**を結合して推論できるようにする（ゼロショット推論のベース）。 | 既存の汎用BigVGAN（事前学習済み）にベクトルを追加しても破綻しないことを確認。 |

### フェーズ B（2026年 Q4）: D&D適応UIとFew-shot学習ループの実装（「牙」の完成）

| タスク | 詳細 | ユーザー体験 |
|--------|------|-------------|
| **B-1** | **メインウィンドウへのD&D受付エリアの実装**（`main_window.py` の`dropEvent`を拡張）。 | フォルダをドラッグすると「この音源をAI学習しますか？」とポップアップ。 |
| **B-2** | **バックグラウンド適応スレッド**の実装。D&D後、UIをブロックせずにA-1〜A-2（特徴抽出→ベクトル生成）を実行。 | プログレスバーに「声の特徴を解析中...」と表示。 |
| **B-3** | **Few-shot Fine-tuningループ**の実装（モードB）。抽出した特徴量と、音源の全WAVを使ってBigVGANの一部レイヤーを**5〜15分だけ追加学習**。 | 「高品質モード」を選んだ場合、完了後に「[音源名] のAI適応が完了しました！」と通知。 |
| **B-4** | 適応済みモデルの**キャッシュ保存**（`.vose_adapt/`フォルダに保存）。次回同じ音源をD&Dしたときは即座に読み込む。 | 2回目以降は待ち時間ゼロで高品質AI歌唱が可能に。 |

### フェーズ C（2027年 Q1〜Q2）: エコシステム化とスケーリング

| タスク | 詳細 |
|--------|------|
| **C-1** | **適応モデルのエクスポート/インポート機能**。ユーザーがチューニングした音源を「.vose_voice」パッケージとして共有可能に。 | コミュニティで「この音源のAI適応モデル、めっちゃいいよ！」と共有できるエコシステムが生まれる。 |
| **C-2** | **Google Colab連携**。ローカルPCのGPUが非力な場合、**ワンクリックでColabに適応処理をオフロード**する機能（API連携）。 | CPU-onlyのユーザーでも高品質適応が可能に。 |
| **C-3** | **適応中のライブプレビュー**。学習が完全に終わる前でも、途中経過のモデルで即座に歌声を試せる「インタラクティブ適応」機能。 | ユーザーがパラメータ（適応強度など）を調整しながらリアルタイムに声質を変えられる。 |

---

## 3. データ戦略（この方式における最重要ポイント）

この「D&D適応型AI」においては、**大規模なマルチ話者データセットは必須ではありません**（むしろ汎用BigVGANのベースモデルがあれば十分）。

代わりに、**「適応の質を決める3要素」**に注力します：

| 要素 | 重要度 | 対策 |
|------|--------|------|
| **ベースBigVGANの品質** | 🔴 最重要 | NVIDIA公式の事前学習モデル（LibriTTS等で学習済み）をベースにする。**ゼロから学習はしない**。 |
| **音響指紋抽出の精度** | 🟠 重要 | 少量の音源（10種類程度）でEncoderを事前学習し、**UTAU音源特有のノイズや非周期性にロバスト**にする。 |
| **Few-shot学習のアルゴリズム** | 🟠 重要 | **Adapter Layer** や **LoRA（Low-Rank Adaptation）** を採用し、ベースモデルの重みをほぼ変えずに軽量な追加パラメータだけを学習させる。 |

**特にLoRAは**: 
- 学習が**極めて速い（5分以内）**
- モデルサイズの増加が**数MB程度**（キャッシュ保存に最適）
- ベースモデルを壊さずに音源固有の「クセ」だけを追加学習できる

→ **VO-SE ProのD&D適応にうってつけの技術**です。

---

## 4. ユーザー体験フロー（完成イメージ）

1. **ユーザーが「重音テト」の音源フォルダをウィンドウにドラッグ＆ドロップ**
2. ポップアップ表示: 
   > 「『重音テト』をAI適応しますか？（高品質モード：約8分 / 高速モード：即時）」
3. ユーザーが「高速モード」を選択 → バックグラウンドで特徴抽出（3秒）完了
4. ユーザーがピアノロールに「ドレミの歌」を打ち込む
5. **「再生」ボタンを押す → 重音テトの声質（エッジの効いた声）を保ったまま、BigVGANによる超滑らかな歌声が再生される！**
6. ユーザーが満足して「WAV書き出し」→ 完成
補足.AI適応したものはディスクに保存してユーザーが残すか削除するか選べるようにする

---

## 5. 成功指標（KPI）／ 技術的チャレンジ

| 項目 | ターゲット | 検証方法 |
|------|------------|----------|
| **適応時間（高速モード）** | **5秒以内**（特徴抽出のみ） | ストップウォッチ計測 |
| **適応時間（高品質モード）** | **10分以内**（LoRA学習） | ストップウォッチ計測 |
| **音質（MOS）** | **WORLD比 +0.8以上**（Synthesizer Vに肉薄） | 盲聴テスト（ABX） |
| **音源の個性再現度** | 元のUTAU音源と**95%以上の類似性**（フォルマント・ブレス感） | スペクトル類似度（コサイン距離） |

**最大の技術的チャレンジ**:
- **UTAU音源は「音素ごとに音量やピッチがバラバラ」**（正規化されていない）という特徴があります。これをそのまま学習させるとAIが混乱するため、**適応前にWAVのRMS正規化とピッチ正規化（平均F0を一定に揃える）** を前処理として必ず入れます。

---

## 6. まとめ：これは「牙」を超えた「最強の武器」になる

**Synthesizer V**は「プロが用意した高品質な歌声DB」で戦います。
**VO-SE Pro（次世代）**は「**ユーザーが所有するあらゆるUTAU音源を、その場でAI音源に変える魔法**」で戦います。

- ユーザーは**新しい音源を買う必要がない**（既存のUTAU音源が無限に使える）。
- 音源製作者は**AI適応用に特別なデータを用意する必要がない**（従来のoto.ini + WAVでOK）。
- コミュニティは**適応済みモデル（.vose_voice）を共有**することで、音源の価値をさらに高められる。

**これこそが、UTAUエコシステムの10年以上の蓄積を一気にAI時代へ引き上げる「革命」です。**

次の一手は、**このLoRA適応ループを `vose_core.cpp` に実装し、Python（`main_window.py`）からシームレスに呼び出せるブリッジを作ること**です。コードの準備はできていますか？それとも、まずはこのアーキテクチャのプロトタイプ（Colabノートブック）から始めますか？
